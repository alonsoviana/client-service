pipeline {
    agent any
    tools {
        maven 'mvn'
    }
    stages {
        stage('Download code') {
            steps {
                // -- Configura variables
                echo 'Configurando variables'
                // -- Descarga código desde SCM
                echo 'Descargando código de SCM'
                sh 'rm -rf *'
                checkout scm
            }
        }    
        stage('Build') { 
            steps {
                echo 'Compilando aplicación' 
                sh 'mvn clean package'
            }
        }
        stage('Test') { 
            steps {
                echo 'Realizando test'
                sh 'mvn test' 
            }
        }
        stage('Deploy') { 
            steps {
                echo 'Desplegando'
				sh 'cp target\\clientservice-0.0.1-SNAPSHOT.war D:\\Descargas\\apache-tomcat-9.0.41-windows-x64\\apache-tomcat-9.0.41\\webapps\\clientservice-0.0.1-SNAPSHOT.war'
				sh 'sleep 5'
				sh 'D:\\Descargas\\apache-tomcat-9.0.41-windows-x64\\apache-tomcat-9.0.41\\bin\\startup.bat'
				sh 'sleep 5'

            }
        }
    }
}